defmodule LifeGame do
  defstruct size: 0, board: [{0, 0}]

  def newGeneration (lifegame = %LifeGame{}) do
  end

  def countAlive (lifegame = %LifeGame{}, x, y) do
  end

  def isAlive (lifegame = %LifeGame{}, x, y) do
  end
end
